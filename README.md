# Portfolio

Static personal site: open `index.html` in a browser or serve the folder with any static file server.

## Local preview

```bash
npx serve .
# or
python3 -m http.server 8080
```

Then open the URL shown in the terminal.
